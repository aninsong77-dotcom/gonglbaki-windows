# 티켓① — Qwen3-4B 엔진 통합

> 선행: 없음 (단독 시작 가능) · 후행: ②③④가 이 티켓의 프롬프트 포맷·모델 경로 위에서 진행

---

## 1. AS-IS (Read로 확인한 현재 코드)

`server/app.py`:
- 605~606행: `LLM_MODEL_FILE = "kanana-1.5-2.1b-instruct-Q4_K_M.gguf"`, `LLM_MODEL_URL`이 `gonglbaki-windows` 저장소 `models` 릴리스 태그를 가리킴.
- 608~609행: `get_llm_model_path()` → `C:\whisper-models\<LLM_MODEL_FILE>` 고정.
- 634~672행: 지시문 상수(`_ANALYZE_SYS_MAP_PLAIN`, `_ANALYZE_SYS_MAP_LABELED`, `_ANALYZE_SYS_REDUCE`) — 문구 자체는 모델 비의존적 한국어 지시문. 단 637행 주석에 "`/no_think`는 Qwen 전용 토큰이라 제거함"이라고 명시돼 있어, 현재 지시문에는 `/no_think`가 없음.
- 760~798행 `_analyze_ask_llm`: 769~772행에서 **Llama3 계열 채팅 템플릿**(`<|start_header_id|>...<|eot_id|>`)으로 프롬프트를 조립. 794~798행에서 응답 후처리 시 `<think>...</think>` 블록 제거, `<|im_end|>`/`<|eot_id|>` 문자열 제거 — **이미 두 계열 종료 토큰을 모두 처리**하고 있음(과거 Qwen→Kanana→(이번에)Qwen 왕복 이력의 흔적).
- 800~823행 `_reduce_partials`: `LIMIT = 16000`자(803행 주석 — "Kanana 토크나이저 실측 약 0.56토큰/자" 기준으로 산정된 값).
- 826~838행 `analyze_status`: `model_size_mb=1452`(하드코딩, Kanana Q4_K_M 실측 용량).
- 841~869행 `analyze_model_download`: SSE 진행률에서도 `total_mb: 1452` 하드코딩(866행).
- 722~758행 `_ensure_llama_server`: `-c 16384`로 llama-server 기동(735~737행 주석 — Kanana 32k 지원·메모리 여유 근거로 설정).

`src/pages/Index.tsx`:
- 320행: 삭제 확인 문구 "약 1.5GB"
- 333행: 다운로드 버튼 라벨 "요약 모델 (1.5GB)"
- 335행: **하드코딩 `2382MB`** — 335행이 335행 자체로 이미 826행 백엔드의 `model_size_mb=1452`와 불일치(기존 버그, §00 overview §1-8). Qwen 교체 시 이 숫자도 실제 Qwen3-4B-Q4_K_M 용량으로 맞춰야 함(중복 수정 포인트 아님 — 이 자리 자체가 이미 틀려 있었음).
- 340행: "온디바이스 언어모델(카카오 Kanana, Apache 2.0 공개 라이선스)" — 모델명·라이선스 문구 전체 교체 필요(Qwen3는 Apache 2.0이 아닌 Qwen 라이선스 — §3 확인 필요).
- 341행: "요약 모델은 선택 설치(1회 1.5GB)로" — 용량 문구.
- 2624행: "요약 모델(약 1.5GB) 1회 다운로드"
- 2627행: `{analyzeDlMb}MB / 1452MB` — 백엔드 `model_size_mb`와 연동된 자리(하드코딩이 아니라 `analyze_status` 응답을 안 쓰고 리터럴 1452를 박아둔 것 — 실제로는 `d.model_size_mb`를 상태로 저장해 쓰지 않고 있음. 이번 기회에 이 자리도 리터럴 제거하고 API 값을 쓰도록 하면 다음 모델 교체 때 이 파일을 또 열 필요가 없어짐 — 선택 개선, 필수 아님이면 문서에만 남김).

## 2. TO-BE

### 2.1 `server/app.py`

```python
# 605~606행 교체
LLM_MODEL_FILE = "qwen3-4b-instruct-Q4_K_M.gguf"   # 실제 파일명은 §3 변환 절차 후 확정
LLM_MODEL_URL = "https://github.com/aninsong77-dotcom/gonglbaki-windows/releases/download/models/" + LLM_MODEL_FILE
```

- 지시문 상수(634~672행): 문구는 유지하되, `/no_think` 토큰을 지시문 끝(또는 시스템 메시지 앞)에 추가 — Kanana 교체 때 뺐던 것을 되돌린다. **§4 안정성 표의 4개 버릇 교정 문구(문장 수 강조·과제 오분류 금지·행동 지어내기 금지·reduce 항목 제한)는 절대 삭제하지 않는다** — 이 문구들은 모델 종류와 무관하게 축어록 요약 자체의 요구사항이다.
- `_analyze_ask_llm`(760~798행): 770~772행 프롬프트 조립부를 Qwen ChatML로 교체.
  ```python
  prompt = (f"<|im_start|>system\n{system}\n/no_think<|im_end|>\n"
            f"<|im_start|>user\n{user}<|im_end|>\n"
            f"<|im_start|>assistant\n")
  ```
  (정확한 `/no_think` 삽입 위치는 Qwen3 공식 템플릿 문서 기준으로 개발 단계에서 재확인 — 시스템 메시지 안/유저 메시지 끝 등 배치가 모델 버전마다 다를 수 있음)
  794~798행 후처리는 그대로 유지(이미 `<think>` 블록 제거 로직이 있어 `/no_think`가 씹혔을 때도 안전망 역할).
- `_reduce_partials`(800~823행): `LIMIT = 16000`을 Qwen 토크나이저 실측치로 재계산 — 개발 단계에서 더미 대본으로 토큰/글자 비율 측정 후 값 조정(§4 안정성 위험 항목).
- `_ensure_llama_server`(722~758행): `-c 16384`가 Qwen3-4B 기준으로도 충분한지 확인(Qwen3는 32k~128k 컨텍스트 지원 계열이 있어 보통 문제 없음 — 다만 4B 모델의 KV 캐시 메모리 사용량이 2.1B 대비 커지므로 저사양 PC에서 컨텍스트 16384가 메모리 부담이 되는지 실측 필요).
- `analyze_status`(826~838행): `model_size_mb=1452` → Qwen3-4B-Q4_K_M 실제 파일 크기(MB)로 교체.
- `analyze_model_download`(841~869행): 866행 `total_mb: 1452` 동일 교체.

### 2.2 `src/pages/Index.tsx`

7개 지점(320·333·335·340·341·2624·2627행) 문구·숫자 일괄 교체. 340행 라이선스 문구는 Qwen3 실제 라이선스명으로 교체(§3에서 확인).

## 3. 모델 출처 검증 (2026-07-20 세션 중 방향 전환 — 아래 §3.0 참조)

기존 원칙(app.py 602~604행 주석): **배포 모델 = 카카오/Qwen 등 공식 원본을 직접 GGUF 변환·양자화한 것만, 제3자 변환본 금지.**

### 3.0 방향 전환 — "우리가 직접 변환해 GitHub에 올린다" → "Qwen 공식 HF GGUF 저장소에서 직접 받는다"

당초 계획(아래 §3 원안)은 Kanana 때 전례("공식 양자화본이 없어 어쩔 수 없이 직접 변환해 우리 GitHub 릴리스에 올림")를 그대로 답습한 것이었다. 그러나 **Qwen은 그 전제 자체가 다르다** — Qwen 팀이 자체 HuggingFace 계정에 `Qwen/Qwen3-4B-GGUF` 저장소로 이미 여러 양자화본(Q4_K_M 포함)을 직접 배포하고 있다(2026-07-20 세션 중 확인). 이는 제3자(Unsloth 등) 변환본이 아니라 **모델 제작사 본인이 낸 파일**이므로 "공식 원본만 배포" 원칙을 그대로 만족하면서도, 우리가 직접 변환·양자화·GitHub 업로드하는 작업 전체가 불필요해진다.

**확인된 사실(실측)**:
- 저장소: `https://huggingface.co/Qwen/Qwen3-4B-GGUF`
- 대상 파일: `Qwen3-4B-Q4_K_M.gguf` (2,497,280,256 바이트 ≈ 2.33GiB)
- 리비전(커밋 해시, URL 고정용): `bc640142c66e1fdd12af0bd68f40445458f3869b`
- SHA256 체크섬(무결성 검증용): `7485fe6f11af29433bc51cab58009521f205840f5b4ae3a32fa7f92e8534fdf5`
- 고정 다운로드 URL: `https://huggingface.co/Qwen/Qwen3-4B-GGUF/resolve/bc640142c66e1fdd12af0bd68f40445458f3869b/Qwen3-4B-Q4_K_M.gguf`
  (브랜치명 `main` 대신 커밋 해시를 넣어, 저장소에 새 커밋이 생겨도 이 URL이 가리키는 파일 내용은 절대 바뀌지 않음 — Kanana 때 없었던 안정성 이점)
- 참고: 우리가 직접 변환·양자화해 만든 파일(`qwen3-4b-official-Q4_K_M.gguf`, 2,497,280,864 바이트)과 위 공식 파일의 크기가 608바이트 차이(메타데이터 저작자 표기 차이 수준)로 사실상 동일 — 같은 원본·같은 양자화 레벨로 만들어졌음을 방증.

**새 절차**:
```
1. LLM_MODEL_FILE/LLM_MODEL_URL을 Qwen 공식 HF 고정 리비전 URL로 설정 (GitHub 릴리스 업로드 불필요)
2. 다운로드 시 SHA256 체크섬 검증 추가(위 해시값 하드코딩) — 다운로드 실패/변조 감지
3. 우리가 직접 변환한 파일(f16·Q4_K_M·Q4_0·Q3_K_M, `요약모델_테스트/`에 보관)은 최종 배포용이 아니라
   "공식 HF 파일이 우리 변환본과 동등 품질인지 확인하는 A/B 대조용"으로만 활용
4. Kanana 대비 회귀 없음 A/B 검증(§4)은 공식 HF에서 받은 실제 배포 대상 파일로 최종 수행
```

**GitHub 2GB 제한 문제 자체가 해소됨**: HuggingFace 다운로드는 GitHub 릴리스 자산 크기 제한을 받지 않으므로, 아래 §3.1(2GB 초과 시 양자화 낮추기/분할 업로드 절차)은 **적용 대상이 없다** — 원안 문서로 남겨두되 "이번 티켓에서는 미적용"으로 표시한다.

**리스크(안전·보안)**: 다운로드 URL이 특정 커밋 해시로 고정되어 있어도, 사용자 최초 실행 시 2.3GB를 새로 받아야 한다는 점은 동일 — 다운로드 중단/재개(resume), 진행률 표시, 완료 후 체크섬 검증 3가지를 완료 조건에 명시한다(§2.1 반영).

### 3.1 (참고용, 이번 티켓 미적용) — GitHub 릴리스 2GB 초과 시 대안이었던 원안

> 아래는 "우리가 직접 변환해 GitHub에 올린다"는 원래 계획 기준으로 작성된 결정 규칙이다. §3.0의 방향 전환으로 다운로드 소스가 GitHub 릴리스가 아닌 Qwen 공식 HF로 바뀌면서 2GB 제한 자체가 없어졌으므로, 이번 티켓에서는 실행하지 않는다. 실제로 1순위(Q4_0→2.20GB, 여전히 초과)·2순위 직전 단계(Q3_K_M→1.93GB, 제한 통과)까지 실측은 완료했고 그 결과물은 A/B 대조 자료로 남겨둔다. 향후 GitHub 릴리스 방식으로 회귀해야 할 상황이 생기면 이 기록을 재사용한다.

```
1순위: 더 강한 양자화(Q4_K_M → Q4_0 또는 Q3_K_M)로 재양자화해 2GB 이하가 되는지 확인
        — 단, §4 A/B 검증(품질 회귀 없음)을 그 양자화 레벨로 다시 통과해야 채택 가능
2순위: 1순위 양자화로도 2GB를 넘으면(드문 경우) GitHub 릴리스를 파일 2개로 분할 업로드
        (예: .gguf.part1 / .gguf.part2) → LLM_MODEL_URL 다운로드 로직에 분할 병합 단계 추가
        — 코드 변경 폭이 커서(다운로드 SSE 진행률 계산도 두 파일 합산으로 바뀜) 최후 수단으로만 사용
3순위: 그래도 안 되면 Git LFS 등 GitHub 릴리스 이외의 무료 호스팅 재검토
        — 단, "카드등록 불필요·완전 무료" 기존 결정 기준(MEMORY project_kanana_engine_swap.md)을
        유지하는 대안만 채택, 유료/카드등록 필요 서비스는 배제
```

### 3.2 엔진 교체 A/B 검증 실패 시 대응 절차 (Warning 대응 — 되돌리기 경로 명시)

§4 완료 조건의 "Kanana 대비 품질 회귀 없음" A/B 비교에서 **만약 Qwen3-4B가 실제로 더미 대본 검증을 통과하지 못하면**(4개 교정 버릇이 Qwen에서 다른 형태로 재발하거나, 새로운 왜곡이 나타나는 경우) 절차 없이 "일단 배포"로 넘어가지 않는다.

```
1. 실패 양상을 구체적으로 기록(어떤 대본·어떤 문장에서 무슨 문제가 재현되는지)
2. 지시문(§2 지시문 상수) 조정으로 해결 가능한 문제인지 우선 시도
   (Kanana 때도 2차 검증에서 지시문 조정만으로 4개 버릇을 교정한 선례가 있음 — 모델 자체
    문제가 아니라 지시문 문제일 가능성부터 배제)
3. 지시문 조정으로도 해결 안 되면: ②③④(대화형 채팅·RAG·사례서랍 연동)는 Qwen3-4B로 계속 진행하되
   "회기 요약(map-reduce)" 기능만 한정적으로 Kanana를 유지하는 이원화 방안을 사용자에게 제시
   (요약은 품질이 검증된 Kanana로, 자유 대화·RAG 질의응답은 Qwen3-4B로 — 모델 2종 상주는
    §03 문서의 메모리 판정 기준과 동일한 원칙으로 재검토 필요)
4. 그래도 채택 불가 판단이면 ①을 보류하고 사용자에게 재보고 — "일단 진행" 임의 결정 금지
```

## 4. 다른 티켓과의 연결

- TICKET-②③④에 주는 것: `_analyze_ask_llm`의 Qwen ChatML 프롬프트 조립 함수(신규 대화 엔드포인트 `/api/chat/send`가 멀티턴 프롬프트를 구성할 때 이 함수의 템플릿 규칙을 그대로 따름 — 단, 멀티턴은 system+여러 turn을 이어붙이는 방식으로 확장 필요, 이 확장 자체는 티켓②의 작업), `get_llm_model_path()`/`_ensure_llama_server()`(모델 상주 서버 재사용, 신규 엔드포인트가 그대로 호출).
- 공유 파일 주의: `server/app.py`의 596~1032행 구간을 이 티켓이 먼저 수정 완료해야 티켓②가 그 아래(1032행 이후 또는 별도 구간)에 신규 라우트를 추가할 때 병합 충돌이 없다.

## 5. 완료 조건 (requirements.md 그대로 + 세부화, §3.0 방향 전환 반영)

```
[ ] LLM_MODEL_FILE/LLM_MODEL_URL을 Qwen 공식 HF 고정 리비전 URL로 교체(GitHub 릴리스 업로드 불필요)
[ ] 모델 출처 검증 완료(§3.0 — Qwen 공식 HF `Qwen/Qwen3-4B-GGUF` 배포본 확인, 제3자 변환본 아님)
[ ] 다운로드 URL 리비전(커밋 해시) 고정 확인 — 저장소 갱신에도 URL이 가리키는 파일이 안 바뀜
[ ] 다운로드 완료 후 SHA256 체크섬 검증(하드코딩된 공식 해시와 대조) — 불일치 시 삭제 후 에러 안내
[ ] 다운로드 중단·재개(resume) 지원 — 2.3GB 다운로드 특성상 필수
[ ] 자체 변환본(f16/Q4_K_M/Q4_0/Q3_K_M) vs 공식 HF Q4_K_M A/B 대조 — 동등 품질 확인(§4)
[ ] _analyze_ask_llm 프롬프트를 Qwen ChatML(/no_think)로 전환
[ ] _reduce_partials의 LIMIT(16000자) Qwen 토크나이저 실측 기준 재검증
[ ] -c 16384 컨텍스트가 Qwen3-4B 기준으로도 안전한지(메모리·속도) 확인
[ ] 동일 더미 대본 A/B 비교 — Kanana 대비 품질 회귀 없음, 4개 교정 버릇 재발 없음
[ ] 조각 20개 이상 실사용 크기로 SSE 전 구간 완주(과거 "빈 결과" 실사고 재발 방지)
[ ] Index.tsx 7개 지점(320·333·335·340·341·2624·2627행) 용량·모델명·라이선스 문구 갱신
[ ] analyze_status의 model_size_mb, analyze_model_download의 total_mb 실제 값으로 교체
[ ] 기존 "빈출 단어"(Kiwi, analyze_words) 기능 무영향 확인(코드 경로 완전 분리 — 686~692행 _analyze_clean_lines만 공유, LLM 미개입)
```
