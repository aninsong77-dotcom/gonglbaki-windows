# IME 커서 사라짐 — 시점 기준 역추적 (2026-08-06)

증상: 축어록 편집 중 커서(빨간 줄)가 안 보이고, 화면 좌상단 구석에 작은 조합창이 뜨며,
글자·기호가 안 써진다. 선택·삭제는 된다. 창을 최소화→복원하면 풀린다.

## 1. 기준점 — 언제부터인가

Claude 세션 기록 13개(34MB) 전수 조사로 확인한 **최초 보고: 2026-07-29 11:51 (한국시각)**.
그 이전 세션에는 이 증상에 대한 언급이 한 번도 없다. 이후 8/3, 8/5(다수), 8/6 반복.

```
6/12 커밋(정상 사용기) ─ 7/15~16 윈도우 업데이트 3건 ─ 7/19 커밋 ─ 7/22경 빌드 ─ ★7/29 최초 보고
```

## 2. 후보에서 제외 — "괜찮던 시절에 이미 있던 것"

6/12 커밋(`ef6da95`)과 현재 소스를 직접 대조해 확인했다. 아래는 **전부 6/12에 동일하게 존재**했다.
증상 없던 기간 내내 있던 코드이므로 증상을 시작시킨 원인이 될 수 없다.

| 후보 | 근거 | 판정 |
|---|---|---|
| Esc `preventDefault` 가로채기 (`Index.tsx` onKey) | 6/12에 글자까지 동일 | 원인 아님 |
| 편집칸이 `<div contentEditable>{l.text}</div>` 구조 | 6/12에 동일 | 원인 아님 |
| 가계도 전역 키 처리(Esc·Space·Backspace) | 6/12에 동일 | 원인 아님 |
| 탭을 unmount 안 하고 `display:none` 으로 숨김 | 6/12에 동일(가계도) | 원인 아님 |
| `.letter-spin` 회전 애니메이션 CSS (7/19 신규) | `converting && !rawText.trim()` 일 때만 렌더 — 편집 중엔 존재 안 함 | 원인 아님 |

→ **2026-08-06 오늘 적용한 수정 A(EditableLine 구조)·B(Esc 조합 가드)는 원인이 아니다.**
   되돌릴 필요는 없으나(각각 독립적 개선), 이것으로 해결됐다고 볼 근거는 없다.

## 3. 남은 후보

### 코드 (7/19 커밋 `10cd286` 에서 들어와 아직 남아 있는 것)
- **`CaseDrawer.tsx` 1751줄 신규** — 사례서랍 탭. `display:none` 으로 숨겨질 뿐 항상 mount 되어 동작
- Index.tsx 추가분 중 잔존분(분석 기능 등). 1차 원인이던 커서 위치 표식·재생 형광펜은 8/5에 제거됨
- `electron-main.cjs` +132 / `preload.cjs` +16 — 사례서랍 파일 IPC (입력과 직접 관련은 안 보임)

### 코드 밖 (아직 배제 못 함)
- **윈도우 업데이트 KB5120102 / KB5101650 / KB5100998 (2026-07-15~16 설치)** — 정상기와 최초 보고 사이 구간에 들어옴
- **한글 입력기가 2개 등록되어 있음**: Microsoft IME(한국어) + 옛한글 계열 1개. "이전 버전 IME 사용" 설정은 꺼져 있음(기본 신형 IME)
- 화면 배율 200% (`devicePixelRatio: 2`)

## 4. 결정적 시험 (제안)

**6/12 커밋 상태로 빌드해 며칠 사용해 본다.**
- 증상이 **나면** → 원인은 코드가 아니라 윈도우/입력기 환경. 코드 수색 중단.
- 증상이 **안 나면** → 원인은 7/19 커밋 잔존분. 사례서랍부터 좁힌다.

비용: 그 기간 동안 사례서랍·분석 기능을 못 쓴다. 증상이 "어쩌다 한 번"이라 최소 며칠 필요.

**무료로 병행 가능한 시험**: 윈도우 설정 → 시간 및 언어 → 언어 및 지역 → 한국어 →
언어 옵션 → Microsoft IME → 옵션 → 호환성 → "이전 버전의 Microsoft IME 사용" 켜기.
되돌리기 쉬우며, 이걸로 증상이 멎으면 입력기 방향이 강하게 지지된다.

## 5. 설치된 진단

`~/gongulbaki_ime_debug.txt` 에 조합 시작 시각·커서 좌표 확보 가능 여부·조상 요소 transform·
편집 중 Esc 누른 시각이 기록된다(글자 내용은 기록 안 함). 이전 기록은
`~/gongulbaki_ime_debug_이전.txt` 로 보관.

판별법: 증상 시각 부근에 `"caretRects":0` 또는 `"anchorAttached":false` 가 있으면
"커서 좌표를 못 구하는 상태"가 실제로 발생한 것. `"why":"편집중Esc"` 가 바로 앞에 있으면 Esc 연관.

## 6. 부수 발견

- `Index.tsx:516` 가이드 투어 문구가 **이미 제거된 기능**(커서 위치 초록 막대·재생 형광펜)을
  아직 설명하고 있다. 사용자에게 거짓 안내 → 문구 수정 필요.
- `Genogram.tsx:310` 전역 키 가드가 `INPUT`·`TEXTAREA` 만 보고 **`isContentEditable` 을 안 본다**.
  축어록 편집 중 Backspace·Delete·Ctrl+Z 가 숨은 가계도로도 전달된다(선택된 노드가 없으면 무해하나
  잠재 버그). 6/12부터 있던 것이라 이번 증상의 원인은 아니다.
